#pragma warning disable CS1591 // Missing XML comment for publicly visible type or member

namespace {{ Namespace }};

[global::System.Runtime.CompilerServices.CompilerGeneratedAttribute]
[global::Sparkitect.DI.CoreContainerConfiguratorEntrypointAttribute]
public class {{ ConfiguratorClassName }} : global::Sparkitect.DI.CoreConfigurator
{
    public override void ConfigureIoc(global::Sparkitect.DI.Container.ICoreContainerBuilder container)
    {
        {% for singleton in Singletons -%}
        container.Register<{{ singleton.FactoryFullName }}>(); {%- unless forloop.last %}
        {% endunless -%}
{%- endfor %}
    }
}