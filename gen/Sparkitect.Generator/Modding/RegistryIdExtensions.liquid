namespace {{ ExtensionsNamespace }};

public static partial class IdExtensions
{
    extension({{ CategoryPascal }}ID)
    {
        public static {{ ModStructName }} {{ ModName }} => default;
    }
}

public readonly struct {{ ModStructName }}
{
{% for entry in YamlEntries -%}
    public global::Sparkitect.Modding.Identification {{ entry.PropertyName }} => global::{{ RegistrationsNamespace }}.{{ RegistryName }}Registrations.{{ entry.PropertyName }};
{% endfor -%}
{% for entry in MethodEntries -%}
    public global::Sparkitect.Modding.Identification {{ entry.PropertyName }} => global::{{ RegistrationsNamespace }}.{{ RegistryName }}Registrations.{{ entry.PropertyName }};
{% endfor -%}
{% for entry in TypeEntries -%}
    public global::Sparkitect.Modding.Identification {{ entry.PropertyName }} => global::{{ RegistrationsNamespace }}.{{ RegistryName }}Registrations.{{ entry.PropertyName }};
{% endfor -%}
}

